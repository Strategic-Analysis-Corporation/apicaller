# Copy this to .env.local and fill in real values
# NEVER commit .env.local — it's in .gitignore
QM_TOKEN=
QM_WEBMASTER_ID=
FISCAL_API_KEY=
EODHD_API_TOKEN=
FMP_API_KEY=

# Optional public provider support recipients for Report Data Issue drafts.
# Format: Provider=email;Provider=email
# Example shape only; do not commit private aliases unless intentionally public.
NEXT_PUBLIC_PROVIDER_SUPPORT_EMAILS=
